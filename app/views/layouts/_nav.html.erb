<nav class="navbar navbar-expand-sm fixed-top navbar-dark bg-info">
    <div class = "container">
        <%= link_to "e-Learning", root_path, class:"navbar-brand brand", id: "logo" %>
        <div class="collapse navbar-collapse">
            <ul class = "navbar-nav ml-auto">
                <% if logged_in? %>
                    <li><%= link_to "Lesson", lessons_path, class: "nav-link" %></li>
                    <li><%= link_to "Members", users_path, class:"nav-link" %></li>
                    <% if (current_user.account_type == 1) %>
                        <li><%= link_to "Manage Lesson", categories_path, class:"nav-link" %></li>
                    <% end %>
                    <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Welcome <%= current_user.name %>!
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <li><%= link_to raw('<i class="fas fa-user-circle"></i> Profile'), current_user, class: "dropdown-item" %></li>
                        <li><%= link_to raw('<i class="fas fa-cog"></i> Settings'), edit_user_path(current_user), class: "dropdown-item" %></li>
                        <li><%= link_to raw('<i class="fas fa-sign-out-alt"></i> Sign Out'), logout_path, method: :delete, class: "dropdown-item" %></li>
                    </ul>
                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                    </ul>
                <% else %>
                    <li class="nav-item">
                        <%= link_to "Signup", new_user_path, class: "nav-link" %>
                    </li>
                    <li class="nav-item">
                        <%= link_to "Login", login_path, class: "nav-link" %>
                    </li>
                <% end %>
            </ul>
        </div>
    </div>
</nav>